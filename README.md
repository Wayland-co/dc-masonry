# D.C. Masonry

The Hardscape Pros

Generated by [@wayland-co/cli](https://github.com/Wayland-co) from brief `dc-masonry` (operator tier).

## Getting started

```bash
pnpm install
pnpm dev
```

The dev server runs at <http://localhost:3000>.

## Environment variables

The scaffolder generated two files at the project root:

- `.env.local.example` — committed template with the keys this site needs
- `.env.local` — your local working copy; **not** committed (`.gitignore`d)

Fill in real values in `.env.local` before deploying.

No integrations are enabled in this brief.

## Updating content

Content lives in `lib/content.ts`. **Do not edit it directly** — it is regenerated by the scaffolder from the brief. To update copy, edit the brief JSON and re-run:

```bash
wayland scaffold --brief <path-to-brief.json> --force
```
